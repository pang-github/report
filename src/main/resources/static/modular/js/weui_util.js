var weUtil = {    confirm:function(msg,fun){        $.confirm(msg, "确认", function() {            fun()        }, function() {            //取消操作        });    },    success:function (msg) {        $.toast(msg);    },    error:function (msg) {        $.toast(msg, 'forbidden');    },    msg:function (msg) {        $.toast(msg, "text");    },    isNotEmpty: function (obj) {        if (typeof obj == "undefined" || obj == null || obj == "" || obj == undefined || obj == " ") {            return false;        } else {            return true;        }    },    isEmpty: function (obj) {        if (typeof obj == "undefined" || obj == null || obj == "" || obj == undefined || obj == " ") {            return true;        } else {            return false;        }    },    close() {        setTimeout(function () {            //这个可以关闭安卓系统的手机            document.addEventListener(                "WeixinJSBridgeReady",                function () {                },                false            );            //这个可以关闭ios系统的手机            WeixinJSBridge.call("closeWindow");        }, 100);    },    // 设置cookie    setCookie(cname, cvalue, exdays) {        var d = new Date();        d.setTime(d.getTime() + (exdays * 24 * 60 * 60 * 1000));        var expires = "expires=" + d.toUTCString();        document.cookie = cname + "=" + cvalue + ";" + expires + ";path=/";    },    getCookie(cname) {        var name = cname + "=";        var decodedCookie = decodeURIComponent(document.cookie);        var ca = decodedCookie.split(';');        for (var i = 0; i < ca.length; i++) {            var c = ca[i];            while (c.charAt(0) == ' ') {                c = c.substring(1);            }            if (c.indexOf(name) == 0) {                return c.substring(name.length, c.length);            }        }        return "";    },    checkCookie(c_name) {        var name;        name = this.getCookie(c_name);        console.log(name);        if (name != null && name != "") {            return true;        } else {            return false;        }    },    getToken(){        return  this.getCookie("token");    },    logout(){        this.clearCookie("token");        location.href = "/weixin"    },    clearCookie(name) {        this.setCookie(name, "", -1);    },    getUrlParam:function (name) {        //  获取地址栏的参数        var url = window.location.search;        //正则表达式筛选        var reg = new RegExp("(^|&)"+name+"=([^&]*)(&|$)");        //匹配参数        var result = url.substr(1).match(reg);        return result ? decodeURIComponent(result[2]):null;    },    catchError:function (res){        setTimeout(function () {            weUtil.msg(res.message)            if(res.code == 700){                weUtil.logout();            }        },100)    },    post:function (url,data,callback) {        $.showLoading("正在加载...");        $.ajax({            type:'POST',            headers: {                'token':weUtil.getCookie("token")            },            url:url,            dataType:'json',            async:false,            data:data,            success:function(res){                if (res.code == 200 | res.code ==0) {                    callback(res.data);                }else{                    weUtil.catchError(res)                }            },            error:function(res){                setTimeout(function () {                    weUtil.msg(res.responseJSON.message)                    if(res.code == 700){                        weUtil.logout();                    }                },100)            },complete:function () {                $.hideLoading();            }        });    }}