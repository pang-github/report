package com.sweet.system.mapper;import com.baomidou.mybatisplus.extension.plugins.pagination.Page;import com.sweet.core.model.system.layTree;import com.sweet.system.entity.Dept;import com.baomidou.mybatisplus.core.mapper.BaseMapper;import org.apache.ibatis.annotations.Param;import org.apache.ibatis.annotations.Select;import java.util.ArrayList;/** * <p> * 部门表 Mapper 接口 * </p> * * @author wxl * @since 2019-11-28 */public interface DeptMapper extends BaseMapper<Dept> {    @Select("select DEPT_ID id,DEPT_NAME title,PARENT_ID pid from t_dept order by ORDER_NUM asc")    ArrayList<layTree> deptTree();    @Select("select count(1) from t_user where dept_id = #{deptId}")    Integer getDeptUserCount(@Param("deptId") String deptId);    Page<Dept> customPageList(Page page, @Param("param") Dept dept);}