package com.sweet.modular.weixin.mapper;import com.sweet.modular.patient.entity.Patient;import com.sweet.modular.record.entity.Record;import com.sweet.system.entity.User;import org.apache.ibatis.annotations.Mapper;import org.apache.ibatis.annotations.Param;import org.apache.ibatis.annotations.Select;import org.apache.ibatis.annotations.Update;@Mapperpublic interface WeiXinMapper {    @Update("update t_user set open_id = '' where open_id = #{openId}")    public int resetOpenId(String openId);    @Select("select * from business_record where patient_id = #{patientId}")    public Record getPatientrRecord(String patientId);}